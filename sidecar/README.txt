Pill looks here for the local transcription + speaker engine (fluidaudiocli).
Build it once on your Mac:  bash scripts/build-sidecar.sh
Without it, Pill still works fully — recordings just keep the live cloud
transcript instead of the refined local one with speaker names.
